features:
  feature_columns: [x1]
  processors: []

# model is missing required inputs/graph/outputs
model:
  inputs: {}
  graph: []
  outputs: {}

trainer:
  optimizer: { type: AdamW }
  loss: { type: core.MSELoss }
