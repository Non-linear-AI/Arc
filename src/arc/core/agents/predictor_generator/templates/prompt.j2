You are an expert at creating predictor specifications for machine learning models. Generate a predictor YAML specification based on the user requirements.

## Task
Create a predictor specification for model "{{ model_id }}" (version {{ model_version }}) based on the user's requirements.

## User Requirements
{{ user_context }}

## Model Information
Model ID: {{ model_id }}
Model Version: {{ model_version }}

{{ outputs_info }}

## Prediction Requirements
{{ requirements_info }}

{% if predictor_examples %}
## Example Predictor Specification

Here's an example predictor specification for reference:

{% for example in predictor_examples %}
**Example: {{ example.name }}**
Purpose: {{ example.user_intent }}

```yaml
{{ example.schema.strip() }}
```

{% if example.explanation %}{{ example.explanation }}{% endif %}
{% endfor %}
{% endif %}

## Predictor Specification Format

A predictor specification defines how to use a trained model for inference with custom output mapping. It should include:

1. **name**: Descriptive name for the predictor service
2. **model_id**: The model identifier to use
3. **model_version**: Specific model version (optional, defaults to latest)
4. **outputs**: Mapping of predictor output names to model output names

### Output Mapping Rules
- Keys are the predictor output names (what your service will return)
- Values are the model output names (what the model actually produces)
- You can map multiple predictor outputs to the same model output
- This allows for flexible naming and multiple views of the same data

### Guidelines
- Use clear, descriptive names for the predictor
- Choose meaningful output names that match the use case
- Consider what outputs would be most useful for the application
- You can create aliases (e.g., 'confidence' and 'probability' both pointing to same model output)

## Output Requirements

Generate ONLY the YAML specification. Do not include any explanations, markdown formatting, or additional text.

The YAML should be valid and follow this exact structure:

```yaml
name: "descriptive_predictor_name"
model_id: "model_identifier"
model_version: 1  # optional, omit for latest
outputs:
  output_name_1: "model_output_name_1"
  output_name_2: "model_output_name_2"
  # additional mappings as needed
```

Generate the predictor specification now: